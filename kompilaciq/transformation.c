#include"transformation.h"
#include<stdlib.h>
#include<string.h>

#define TRANSFER(A) long num = atoi(str1);\
if(num!=0){\
  something.result = num;\
  strcpy(something.error, "\0");\
}\
else{\
  something.result = 0;\
  strcpy(something.error, "Invalid input string");

struct transformation trstr() {
    struct transformation something;
    char str1[100] = "756";
    char str2[100] = "ab34n7";
    TRANSFER(str1);
    TRANSFER(str2);
  }
  return something;
  }
}